import ReactDOM from 'react-dom/client'
import './index.css'

ReactDOM.createRoot(document.querySelector('.root')).render(
  <>
    <h1>Tasks:</h1>
    <ol>
      <li>Make a header component and render it in App</li>
      <li>Make a body component and render it in App</li>
      <li>Make a card component and render it inside the body component</li>
      <li>pass the names of the theme team members as an array in the card component</li>

    </ol>
  </>
)

